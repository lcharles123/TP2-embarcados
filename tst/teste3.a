; ler dois numeros e retornar a potencia de um elevado a outro
read A0 ;A0 ^ A1
read A1
store A0 base
store A1 exp

jmpz A1 zeroexp
call mull
jump final

mull: push A0
load_c A2 1
load A1 exp
subtract A1 A2
store A1 exp ; sempre empilha A0; call; A0...
jmpz A1 continua
call mull ;chamadas recursivas
continua: load A0 res
pop A1
multiply A0 A1
store A0 res
return

zeroexp: load A0 res
final: load A0 res
write A0
stop

base: .data 2 0 ;
exp: .data 2 0 ; expoente, contador para quantidade multiplicacoes
res: .data 2 1 ; resultado final
